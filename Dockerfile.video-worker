FROM node:22-alpine
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci
RUN apk add --no-cache ffmpeg font-noto
COPY . .
RUN npm run build
CMD ["node", "scripts/railway-worker-entrypoint.mjs"]
